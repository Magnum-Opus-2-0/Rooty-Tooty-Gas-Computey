import React from 'react';

class FilterPopup extends React.Component {
    render() {
        return (
            <div>FilterPopup {this.props.name}</div>
        );
    }
}

export default FilterPopup;